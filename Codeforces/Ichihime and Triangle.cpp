#include <bits/stdc++.h>

using namespace std;
#define endl "\n"

int t, a, b, c, d;

int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    cin >> t;

    while (t--) {
        cin >> a >> b >> c >> d;

        cout << b << " " << c << " " << c << endl;
    }

    return 0;
}